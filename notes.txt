Layout - fundalul + poza
	Header - titlul + iconita tema
	TodoApp
		WriteBar - insert input
		TodoList - items + filtru


TODOS:

forbid extremely long words inside the text input - if they do not have spaces, they will not break on new line causing overflow.
find a decent character limit for the whole input string (spaces included)

Add error message when any of the input constraints are not respected

Add drag and drop functionality.

Add add / remove item from list animation effect. Now everything is too sudden.

Figure out some nice default messages for the already existing items:
"Hello"
"Welcome to TODOs App"
"Create new items, hit the checkbox when they are done"
"Have fun!"
